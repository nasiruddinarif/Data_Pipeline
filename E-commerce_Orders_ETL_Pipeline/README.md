# 🛍️ E-commerce Orders ETL Pipeline (Python)

A simple end-to-end ETL (Extract, Transform, Load) pipeline project for e-commerce order data — built in **Python** using **Pandas** and **Logging**.

---

## 🚀 Project Overview

This project demonstrates a clean, modular ETL process:
1. **Extract** — Reads raw CSV files (`orders.csv` and `customers.csv`)
2. **Transform** — Merges, cleans, enriches, and adds derived columns
3. **Validate** — Checks data quality (missing, duplicates, invalid)
4. **Load** — Writes validated output to `cleaned_orders.csv`
